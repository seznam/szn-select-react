{
  "name": "@jurca/szn-select-react",
  "version": "1.4.1",
  "description": "Szn-select integration for React projects.",
  "main": "index.js",
  "scripts": {
    "test": "eslint --max-warnings=0 *.js && jest",
    "build": "gulp",
    "prepublishOnly": "echo Run \\'npm run publish-package\\' instead && exit 1",
    "publish-package": "git push && git push --tags && npm run build && npm publish --access public --ignore-scripts dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jurca/szn-select-react.git"
  },
  "keywords": [
    "a11y",
    "select",
    "web",
    "component",
    "components",
    "szn",
    "seznam",
    "seznam.cz"
  ],
  "author": "Martin Jurča <martin.jurca@firma.seznam.cz>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jurca/szn-select-react/issues"
  },
  "homepage": "https://github.com/jurca/szn-select-react#readme",
  "peerDependencies": {
    "react": "*",
    "prop-types": "*"
  },
  "devDependencies": {
    "@jurca/szn-select": "^1.4.0",
    "babel-preset-env": "^1.6.1",
    "del": "^3.0.0",
    "eslint": "^4.18.1",
    "eslint-config-fbjs-opensource": "^1.0.0",
    "gulp": "github:gulpjs/gulp#4.0",
    "gulp-babel": "^7.0.1",
    "gulp-replace": "^0.6.1",
    "jest": "^22.4.2"
  }
}
